QDRANT_URL=http://localhost:6333
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASS=CHANGE_ME_NOW

# 0 keeps text embedding on CPU so memory does not compete with live LLM/ASR GPU use.
# Raise this only if you explicitly want the embedding GGUF offloaded by llama.cpp.
LISTENBURY_TEXT_EMBEDDING_GPU_LAYERS=0
